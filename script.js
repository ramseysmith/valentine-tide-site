/* =========================================================
   Valentine Tide — script
   ---------------------------------------------------------
   Phase 1: configuration + bootstrap only.
   Email validation + submit handling arrive in Phase 3.
   ========================================================= */

"use strict";

/*
 * Form endpoint.
 * Swap this for a real Formspree, Shopify, or custom URL later.
 * Leave as the placeholder for now — the submit handler (Phase 3)
 * will treat the placeholder as "not configured" and simulate success.
 */
const FORM_ENDPOINT = "REPLACE_WITH_FORM_ENDPOINT";

/* Run once the DOM is parsed. */
document.addEventListener("DOMContentLoaded", () => {
  // Phase 2+ wiring goes here.
});
